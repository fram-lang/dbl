(library
 (name config)
 (modules config))

(executable
  (name dbl)
  (modes byte exe)
  (public_name dbl)
  (libraries interpLib parser typeInference toCore)
  (modules dbl eval pipeline typeErase))
