(library
 (name dblConfig)
 (modules dblConfig))

(executable
  (name dbl)
  (modes byte exe)
  (public_name dbl)
  (libraries interpLib dblParser typeInference toCore unix)
  (modules dbl eval pipeline typeErase))
