(library
 (name configuration)
 (modules configuration))

(executable
  (name dbl)
  (modes byte exe)
  (public_name dbl)
  (libraries interpLib parserDbl typeInference toCore)
  (modules dbl eval pipeline typeErase))
