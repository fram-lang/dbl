data Result E A = Error of E | Ok of A

let test_bad x =
  match x with
  | Error _ | Ok a => a
  end
# @stderr:error: Or-pattern branches must bind the same variables, but variable a is not bound in all branches
