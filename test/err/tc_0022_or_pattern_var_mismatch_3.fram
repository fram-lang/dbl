data Option A = None | Some of A

let bad3 x =
  match x with
  | None | Some x => x
  end
# @stderr:error: Or-pattern branches must bind the same variables, but variable x is not bound in all branches
