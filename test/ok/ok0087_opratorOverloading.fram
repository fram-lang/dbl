method add = (extern dbl_addInt : Int -> Int -> Int)
method add = (extern dbl_strCat : String -> String -> String)

let (+) {T, type U, method add : T -> U} (x : T) = x.add

let _ = 2 + 2
let _ = "a" + "b"
